# Name etc

# Setting up NIST
Go to 
```data/raw/```
and unzip nist_IR(1).zip into IR/

Then the IR/ folder with nist_compounds.csv and nist_ir_info.csv for metadata are all you need!
You can run files in ```src/data/``` directly now.